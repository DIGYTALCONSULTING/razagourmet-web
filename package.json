{
  "name": "raza-gourmet",
  "type": "module",
  "private": true,
  "scripts": {
    "optimize:images": "node scripts/optimize-images.mjs",
    "build": "node scripts/optimize-images.mjs && nuxt build",
    "dev": "nuxt dev",
    "generate": "node scripts/optimize-images.mjs && nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "nuxt": "^4.4.2",
    "vue": "^3.5.30",
    "vue-router": "^5.0.3"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.14.0",
    "sharp": "^0.34.5"
  }
}
